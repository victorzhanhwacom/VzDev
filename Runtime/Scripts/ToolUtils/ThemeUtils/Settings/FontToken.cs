namespace VzDev.ToolUtils.ThemeUtils
{
    /// <summary>
    /// 定義字體 Token 名稱（等同 CSS 的 --font-body, --font-heading...）
    /// </summary>
     public enum FontToken
    {
        Body,
        Title1,
        Title2,
        Headline,
        DisplayLarge,
        Caption
    }
}
